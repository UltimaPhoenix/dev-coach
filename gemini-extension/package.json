{
  "name": "devcoach-gemini-extension",
  "private": true,
  "description": "Pins the published devcoach version the extension installs once into its data dir (see scripts/launch.mjs).",
  "dependencies": {
    "devcoach": "1.0.1"
  }
}
