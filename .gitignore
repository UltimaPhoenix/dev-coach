# Dependencies
node_modules/

# Build output
dist/
dist-mcpb/
dist-plugin/
mcpb/build/
*.tsbuildinfo

# MCPB signing keys (generated by `npm run mcpb:sign --self-signed`)
mcpb/cert.pem
mcpb/key.pem

# Test coverage
coverage/

# Editor / OS / local agent config
.DS_Store
.idea/
.vscode/
.claude/
.playwright/
.playwright-cli/
.playwright-mcp/

# Local scratch
todo

# Local-only docs (e.g. the plugin release runbook) — never committed
*.local.md

# Secrets
.env
.envrc
.npmrc
